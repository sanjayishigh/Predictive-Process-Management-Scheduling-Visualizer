Flask==3.0.3
# NumPy 2.x is required to unpickle dnn_priority_scheduler.pkl (BitGenerator state).
numpy>=2.0.0,<3
# pandas>=2.2.2 supports NumPy 2 on Python 3.12 (2.2.1 capped NumPy<2).
pandas>=2.2.2
joblib==1.3.2
# Match sklearn used when serializing the model (avoids InconsistentVersionWarning).
scikit-learn==1.8.0
